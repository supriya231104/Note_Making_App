# Note_Making_App
A frontend-only note-taking application built using **ReactJS**. The project demonstrates clean **component-based architecture**, **state management using React hooks**, and proper **UI validation**, without using Redux Toolkit, Context API, or a backend.
